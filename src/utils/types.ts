export type UserProgramInfo = {
  userTrainingId: number;
  user_email: string;
};

export type EmailContent = {
  subject: string;
  programName: string;
  header: string;
  body: string;
  footer: string;
};
